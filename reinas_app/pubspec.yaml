name: reinas_app
description: "Elección de las Reinas"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.0
  rxdart: ^0.27.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Aquí es donde debe ir la sección de assets
  assets:
    - assets/banner.png
